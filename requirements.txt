# Core framework
flask>=3.0,<4.0
flask-sqlalchemy>=3.1,<4.0
flask-login>=0.6,<1.0
flask-migrate>=4.0,<5.0
flask-wtf>=1.2,<2.0
flask-limiter>=3.5,<4.0
flask-mail>=0.10,<1.0

# Database
psycopg2-binary>=2.9,<3.0

# WSGI server
gunicorn>=21.0,<22.0

# Background scheduler
apscheduler>=3.10,<4.0

# Security / tokens
itsdangerous>=2.1,<3.0
werkzeug>=3.0,<4.0

# Excel export (optional — used by admin reports)
openpyxl>=3.1,<4.0

# File type validation (optional — falls back to mimetypes if absent)
# python-magic-bin is the Windows-compatible build; on Linux use python-magic

# Google Drive backup (optional — only needed when GDRIVE_CREDENTIALS is set)
# Uncomment when you configure Google Drive:
# google-api-python-client>=2.100,<3.0
# google-auth>=2.23,<3.0
google-api-python-client>=2.100,<3.0
google-auth>=2.23,<3.0
redis>=4.0,<6.0

requests>=2.31,<3.0
python-magic>=0.4,<1.0
